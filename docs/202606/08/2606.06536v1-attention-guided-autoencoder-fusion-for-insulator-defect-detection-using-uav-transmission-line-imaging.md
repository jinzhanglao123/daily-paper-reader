---
title: Attention-Guided Autoencoder Fusion for Insulator Defect Detection Using UAV Transmission-Line Imaging
title_zh: 基于注意力引导自编码器融合的无人机输电线路成像绝缘子缺陷检测
authors: "Malak Allam, Khaled Shaban, Ali Hamdi"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.06536v1"
tags: ["query:uav-drl"]
score: 10.0
evidence: 基于无人机图像的输电线路绝缘子缺陷检测，采用注意力引导自编码器融合
tldr: "针对无人机输电线路绝缘子缺陷检测中类别不平衡、尺度变化大及缺陷微小的问题，提出了注意力引导自编码器增强YOLO框架AE-YOLO。该框架在特征金字塔颈部嵌入轻量瓶颈自编码器保留异常敏感信息，结合CBAM注意力抑制背景干扰，并通过方差最大化正则化提升特征多样性。训练采用Focal Loss、CIoU损失和自编码器正则化，推理引入加权框融合与置信度提升。在绝缘子缺陷数据集上，AE-YOLO使用EfficientNetV2骨干达95.1% mAP@0.5，超最优YOLO模型5个点，精度96.4%，召回93.8%。证明其有效应对绝缘子缺陷检测挑战，为无人机巡检提供可扩展方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06536v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1875, \"height\": 488, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06536v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1891, \"height\": 1002, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06536v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06536v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06536v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 933, \"height\": 427, \"label\": \"Table\"}]"
motivation: 无人机输电线路绝缘子图像中缺陷类别极不平衡、尺度差异大且目标微小，传统检测器难以有效定位与识别。
method: 提出AE-YOLO，在FPN-PAN颈部嵌入轻量自编码器保留异常细节，结合CBAM注意力与方差最大化正则化，并采用Focal+CIoU损失及WBF推理融合策略。
result: "在绝缘子缺陷数据集上，AE-YOLO以EfficientNetV2为骨干获95.1% mAP@0.5，精度96.4%，召回93.8%，较最强YOLO基线提升5.0点mAP与6.7点召回。"
conclusion: AE-YOLO有效解决绝缘子缺陷检测难题，性能优越，可实际部署于无人机输电线路巡检。
---

## 摘要
由于无人机（UAV）影像中高压输电线路绝缘子缺陷检测存在严重的类别不平衡、大尺度变化以及缺陷实例空间范围小等问题，自动缺陷检测仍然面临挑战。为解决这些问题，本文提出 AE-YOLO，一种注意力引导的自编码器增强 YOLO 框架，用于鲁棒的绝缘子缺陷检测。该架构在特征金字塔网络-路径聚合网络（FPN-PAN）颈中集成轻量级瓶颈自编码器，以在多尺度特征融合过程中保留异常敏感信息。骨干网络全程使用卷积块注意力模块（CBAM），增强特征区分能力并抑制背景干扰。该框架还引入了一种方差最大化自编码器正则化策略，促使学习到多样化且对缺陷具有判别力的潜在表示。网络训练采用联合目标函数，结合焦点损失、完整交并比（CIoU）损失和自编码器正则化，以解决前景-背景不平衡并提高定位精度。在推理阶段，加权框融合（WBF）结合 YOLOv8、YOLOv10 和 YOLO11 的预测；自编码器引导的置信度增强机制提高了对稀有缺陷类别的敏感度。在绝缘子缺陷检测数据集上的实验表明，采用 EfficientNetV2 主干网络的 AE-YOLO 在 0.5 交并比阈值下达到 95.10% 的 mAP，96.40% 的精确率和 93.80% 的召回率，比最强的 YOLO 系列基线在 mAP@0.5 上高出 5.0 个百分点，召回率高出 6.7 个百分点。这些结果证实了该框架的有效性和适应性，使其成为基于无人机输电线路巡检和缺陷监测的实用且可扩展的解决方案。

## Abstract
Automated defect detection in high-voltage transmission-line insulators remains challenging due to severe class imbalance, large scale variation, and the small spatial extent of defect instances in Unmanned Aerial Vehicle (UAV) imagery. To address these challenges, this paper proposes AE-YOLO, an Attention-Guided AutoEncoder-Enhanced YOLO framework for robust insulator defect detection. The architecture integrates lightweight bottleneck autoencoders within a Feature Pyramid Network-Path Aggregation Network (FPN-PAN) neck. This preserves anomaly-sensitive information during multi-scale feature fusion. Convolutional Block Attention Modules (CBAM) are used throughout the backbone, enhancing feature discrimination and suppressing background interference. The framework also introduces a variance-maximizing autoencoder regularization strategy, which encourages diverse, defect-discriminative latent representations. The network trains using a unified objective that combines focal loss, Complete IoU (CIoU) loss, and autoencoder regularization to address foreground-background imbalance and improve localization accuracy. During inference, Weighted Boxes Fusion (WBF) combines predictions from YOLOv8, YOLOv10, and YOLO11. An autoencoder-guided confidence boosting mechanism improves sensitivity to rare defect categories. Experiments on the Insulator-Defect Detection dataset show that AE-YOLO with an EfficientNetV2 backbone achieves 95.10 percent mAP at 0.5, 96.40 percent precision, and 93.80 percent recall. This performance surpasses the strongest YOLO-family baseline by 5.0 points in mAP at 0.5 and 6.7 points in recall. These results confirm the effectiveness and adaptability of the framework. The model is a practical and scalable solution for UAV-based transmission-line inspection and defect monitoring.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：高压输电线路绝缘子在长期电应力、机械荷载及恶劣环境作用下，易出现自爆、开裂、污秽闪络等缺陷，导致绝缘下降甚至停电。传统人工巡检与规则图像法成本高、难以规模化；无人机（UAV）虽能低成本采集高分辨率图像，但该类图像面临**严重类别不平衡**（缺陷目标占比极小）、**大尺度变化**（绝缘子串尺度悬殊，缺陷仅占几十像素）、**复杂背景干扰**（植被、杆塔、阴影等）等挑战。
- **核心问题**：如何在UAV输电线路绝缘子图像中，准确检测污染闪络、破损等微小且稀有的缺陷，同时维持高召回与高精度。
- **整体含义**：本文提出AE‑YOLO，一种融合注意力机制与轻量自编码器的YOLO检测框架，专门为**保留异常判别特征、缓解类别不平衡、增强微小缺陷敏感度**而设计，旨在提供面向实际UAV巡检的可扩展、高鲁棒检测方案。

### 2. 方法论

**AE‑YOLO整体架构**包含四个核心部分：可配置的注意力增强主干、特征金字塔自编码器（FPA）颈、多尺度检测头、以及带置信度增强的加权框融合（WBF）集成推理。

#### 2.1 注意力增强主干（Backbone）
- 支持多种主干网络：EfficientNetV2、MobileNetV3、ResNet50、DenseNet201、ConvNeXt‑Tiny。
- 在每次下采样后嵌入**卷积块注意力模块（CBAM）**，通过串行通道与空间注意力，强化缺陷区域特征，抑制背景干扰。
- 提取三个尺度特征图**P3、P4、P5**，对应不同感受野。

#### 2.2 特征金字塔自编码器颈（FPA Neck）
- **主要创新**：在每个金字塔级别嵌入一个轻量瓶颈自编码器，旨在保留异常敏感信息。
- **实现细节**：
  - 1×1卷积将P3、P4、P5统一投影至128通道。
  - 每个FPA模块：  
    **编码器**：1×1卷积压缩至瓶颈维度\(C_b=\lfloor128\cdot 0.25\rfloor=32\)，经BN+SiLU激活得编码Z；  
    对Z施加**通道注意力门控**（全局平均池化→两层1×1 MLP→Sigmoid），生成注意力权重并与Z逐元素相乘，得\(\hat{Z}\)；  
    **解码器**：1×1卷积恢复至128通道，经BN输出\(\hat{F}\)；  
    **残差融合**：\(F^+ = F + \alpha \cdot \hat{F}\)，\(\alpha\)初始化为0.1且端到端学习，稳定训练并逐步引入自编码器信号。
  - **训练正则化**：对三个尺度的瓶颈编码施加**方差最大化损失\(L_{ae}\)**，鼓励空间和批次维度上的方差，同时加L2正则防止激活爆炸，防止编码坍缩为平凡表示。
- 增强后的特征经**FPN‑PAN双路径**（自上而下与自下而上）进一步融合，确保细粒度空间细节与高级语义信息同时存在。

#### 2.3 检测头与训练损失
- **检测头**：共享的深度可分离3×3卷积+SiLU，后接1×1卷积输出\(3\times (N_c+5)\)通道（3锚点）。
- **联合总损失**：  
  \[
  L = \lambda_{cls}L_{cls} + \lambda_{box}L_{box} + \lambda_{ae}L_{ae}
  \]
  - **分类损失**：Focal Loss（\(\alpha=0.25, \gamma=2.0\)），降低易分背景样本权重。
  - **定位损失**：CIoU Loss（CIoU = 1 – IoU + 中心距离惩罚 + 纵横比一致性项），提升小目标框回归精度。
  - **自编码器正则化\(L_{ae}\)**：三项瓶颈的方差最大化＋L2正则。

#### 2.4 集成推理与AE引导置信度增强
- **异常评分**：对每张测试图，利用FPA瓶颈的**空间方差 × 平均激活绝对值**的乘积得到异常分数\(s\)（高激活、高方差表示存在异常缺陷）。
- **置信度提升**：\(\beta = 1 + \min(0.25, 0.15 \cdot s)\)，将分数线性映射为\[1.0, 1.25\]的乘性因子，提升缺陷图的检测分数，而清洁图几乎不变。
- **加权框融合（WBF）**：融合YOLOv8、YOLOv10、YOLO11的预测框（权重与各模型验证mAP50成正比），替代传统NMS，保留重叠小目标。

### 3. 实验设计

- **数据集**：公开的**绝缘子缺陷检测数据集（Insulator‑Defect Detection Dataset）**，包含**1600张UAV图像**，标注三类对象：
  - 绝缘子（1832个实例，平均面积18.58%，最小0.46%）
  - 污染闪络（2448个实例，平均0.22%，最小0.01%）
  - 破损（1093个实例，平均0.59%，最小0.03%）
  - 存在严重类别不平衡和极小目标（最小26×29像素）
- **划分**：训练1296张，验证144张，测试160张（约81:9:10）。
- **对比基准**：
  - **YOLO系列基线**：YOLOv7、YOLOv8、YOLOv10、YOLO11、YOLO26。
  - **两阶段方法**：Faster R‑CNN（mAP 86.2%），以及文献中的改进YOLOv7（93.8%）。
- **评估指标**：Precision（P）、Recall（R）、mAP@0.5、mAP@0.5:0.95。
- **AE‑YOLO变体**：使用5种主干网络——MobileNetV3、ResNet50、DenseNet201、ConvNeXt‑Tiny、EfficientNetV2。

### 4. 资源与算力

- 论文**未明确提及GPU型号、数量、单次训练时长或计算量（FLOPs）等具体资源消耗**。仅在讨论中提及轻量版本（MobileNetV3）适合资源受限场景，但无定量推理速度或算力对比，这是实验分析中的一个缺失。

### 5. 实验数量与充分性

- **主要实验组数**：
  - 5种主干下的AE‑YOLO变体（含自适应对比各自的基YOLO模型，但原文主要对比统一的YOLO基线，未给出每种主干对应的无FPA基线的详细数据）。
  - 1组与现有方法的总体对比表（包括Faster R‑CNN、YOLOv5s、YOLOv7等）。
  - 虽提及CBAM、WBF、自编码器正则化等模块，但**未提供系统化的消融实验**（如移除FPA、移除CBAM、替换损失项等），无法量化各模块的独立贡献。
- **公平性**：所有模型使用相同的数据划分、输入尺寸（640×640）、数据增强，具备一定的可比性；但不同YOLO版本的内在差异可能导致部分增益来自基线版本差异，而非纯架构优化。
- **充分性**：从5种骨干网覆盖范围看，实验展现了框架的鲁棒性与可扩展性；但缺少消融研究、推理速度测试以及跨数据集验证，充分性有提升空间。

### 6. 主要结论与发现

- AE‑YOLO在所有主干上均超越YOLO基线，**EfficientNetV2版本取得最优结果**：95.10% mAP@0.5、96.40% P、93.80% R。
- 相比最强基线YOLOv7，mAP@0.5提升**5.0个百分点**，召回率提升**6.7个百分点**，说明框架对困难、稀有缺陷的恢复能力显著。
- **召回率改善**尤其突出，对污染闪络和破损等微小目标敏感度更高，证实FPA模块能有效保留异常判别特征。
- 通过联合损失设计（Focal + CIoU + 自编码器正则化），同时提高了精确率与召回率，无需像传统方法那样折中。
- 该框架**不受主干网络限制**，在轻量级模型上也能取得有竞争力的召回，适合边缘部署。

### 7. 优点

- **方法创新性**：首次在YOLO颈部引入轻量瓶颈自编码器作为特征正则化器，结合方差最大化损失，形成保留异常敏感信息的独特方案，区别于传统FPN仅做语义聚合。
- **端到端联合优化**：Focal Loss、CIoU、自编码器正则化三者统一，分类、定位、异常特征保留协同工作，设计合理。
- **推理阶段增强**：自编码器引导的置信度提升和WBF融合，进一步利用瓶颈编码的异常信号，提高小目标召回，且不增加训练复杂度。
- **极强的可扩展性和工程价值**：支持多种流行主干，轻量版仍优于多数基线，具备从低算力边缘到高性能服务器的适配潜力。
- **显著性能提升**：相对现有先进方法，尤其在召回率上大幅度进步，对安全关键应用（避免漏检缺陷）意义重大。

### 8. 不足与局限

- **缺乏速度/计算量数据**：未给出推理延迟、参数量、FLOPs或FPS，难以评估实时性和部署可行性。
- **无消融实验**：无法清晰分辨CBAM、FPA、WBF、AE正则化各自的贡献，削弱了方法的说服力。
- **数据集单一**：仅在一个公开绝缘子数据集上验证，未在其他类型绝缘子、不同环境（光照、气象条件）或电网设备上测试，泛化性待检验。
- **并未独立验证CBAM或Transformer融合**：文中提到类似模块，但未对比不加CBAM的变体，也未给出与标准FPN‑PAN的直接对比。
- **WBF权重依赖验证集mAP**：在未知分布下可能需重新标定，实际部署的鲁棒性未深入探讨。

（完）
